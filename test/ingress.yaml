apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test2-nginx-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: test-nginx2.local
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: nginx2
                port:
                  number: 8088
