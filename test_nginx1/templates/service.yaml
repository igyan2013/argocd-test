apiVersion: v1
kind: Service
metadata:
  name: {{ include "test_nginx1.fullname" . }}
  labels:
    {{- include "test_nginx1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test_nginx1.selectorLabels" . | nindent 4 }}
